// Root layout — next-intl pattern. Real <html> is in [locale]/layout.tsx.
// This file exists only because Next.js requires it.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
